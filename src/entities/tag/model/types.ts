export interface Tag {
  id: string;
  name: string;
  slug: string;
  description: string;
  postCount: number;
}

export interface TagWithCount {
  id: string;
  name: string;
  slug: string;
  description: string;
  postCount: number;
}
